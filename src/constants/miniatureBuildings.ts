import { MACRO_BUILDING_STAGES } from './buildingStages';

/** Same 27-stage ladder as focus categories; each resist brick counts as one brick. */
export const MINIATURE_BUILDING_STAGES = MACRO_BUILDING_STAGES;

export const MINIATURE_SCALE = 1;
