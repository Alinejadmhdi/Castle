/** Stub for future social features. See docs/FUTURE_FEATURES.md */
export const socialService = {
  async getFeed(): Promise<never[]> {
    return [];
  },
  async getLeaderboard(): Promise<never[]> {
    return [];
  },
};
